async function fetchSteps() {
    let stepObj = {
        dateArr: ['m','t','w','th','f','s','su'],
        stepArr: [0,1,2,3,4,5,6,7]
    }

    return stepObj;
}

export default fetchSteps;